# CLMS Client (Vite + React)

Quick start (frontend)

```bash
cd client
npm install
npm run dev
```

Default dev server: http://localhost:5173

Set `VITE_API_URL` to point to the backend API if different from http://localhost:4000/api
